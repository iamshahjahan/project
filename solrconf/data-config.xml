<dataConfig> 
 <dataSource type="JdbcDataSource" driver="com.mysql.jdbc.Driver" url="jdbc:mysql://localhost:3306/project" user="root" password="khan" batchSize="1" />  
 <document name="ques">   
  <entity name="ques" query="select questions.q_id,title,description, group_concat(tags.name) as tname from questions inner join tags inner join question_tags on question_tags.q_id=questions.q_id and tags.tag_id=question_tags.tag_id group by questions.q_id" transformer="RegexTransformer">    
   <field column="q_id" name="id"/>
   <field column="title" name="q_title"/>
   <field column="description" name="q_desc"/>
   <!--field column="tag_id" name="tag_id"/--> 
   <field column="tname" name="tagname" splitBy = ","/>   
   <!--field column="name" name="tagname"/-->    
  </entity>  
 </document>
</dataConfig>